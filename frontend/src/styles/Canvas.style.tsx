import styled from 'styled-components'

const canvasWidth = '99.5%';
const canvasHeight = canvasWidth;
export const CanvasStyled = styled.canvas`
	
	position: relative;
	width: ${canvasWidth};
	height: ${canvasHeight};
	`